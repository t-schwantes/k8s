apiVersion: v1
kind: Service
metadata:
  namespace: jupyterhub
  name: proxy-api-nodeport
spec:
  type: NodePort
  ports:
  - port: 8001
    nodePort: 32123
    targetPort: 8001
  selector:
    app: jupyterhub
    component: proxy
    release: jupyterhub
